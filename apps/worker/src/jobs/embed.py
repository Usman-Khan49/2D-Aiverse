# Placeholder for embedding job
# This will be used to generate vector embeddings for the transcript/summary
# to enable semantic search later.

def run_embedding_job(content_id, content_type, text):
    """
    Generates and stores embeddings for the given text.
    """
    print(f"[*] Embedding job started for {content_type}: {content_id}")
    # TODO: Implement embedding logic (e.g. using Gemini embeddings or local model)
    pass
